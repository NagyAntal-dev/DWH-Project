name: crypto_dw
version: "1.0.0"
config-version: 2

profile: crypto_dw

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

log-path: "/tmp/dbt-logs"
target-path: "/tmp/dbt-target"
packages-install-path: "/tmp/dbt-packages"

clean-targets:
  - /tmp/dbt-target
  - /tmp/dbt-packages

models:
  crypto_dw:
    staging:
      +materialized: view
    marts:
      +materialized: table
